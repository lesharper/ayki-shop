import React from 'react';

const Favorites = (props) => {
    return (
        <div className="flex min-h-screen justify-center items-center">
            <span className="text-black text-6xl font-bold">ИЗБРАННЫЕ</span>
        </div>
    );
}

export default Favorites;
